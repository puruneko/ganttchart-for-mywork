
# editor側

- ツリーパネルのタスク名・セクション名をクリックで、ganttは今まで通りその要素が見えるようスクロール（ライブラリ標準機能）、それと同時にエディタの該当箇所にカーソル移動（ganttのバークリックと同様）。セクション名クリックでも発火

# gantt側

- 今の線を中央に表示するようスクロールするボタンを設置
- 右クリックスクロール移動はバー上でもセクションエリア上でもどこでもできるようにする